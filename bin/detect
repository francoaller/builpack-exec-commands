#!/bin/sh

# this pack is valid for apps with a heroku_bash.sh in the root
if [ -f $1/heroku_bash.sh ]; then
  echo "heroku_bash.sh founded"
  exit 0
else
  echo "heroku_bash.sh not founded"
  exit 1
fi
