#!/bin/sh

indent() {
  sed -u 's/^/       /'
}

echo "-----> Found a heroku_bash.sh"

if [ ! -s $1/heroku_bash.s ]; then
  echo "hello.txt was empty"
  exit 1
else
  echo "hello.txt is not empty, here are the contents" | indent
  cd $1
  ./heroku_bash.s
fi | indent